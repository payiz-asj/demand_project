需求表：
- 自动根据天气信息，节日信息更换UI风格，亦可自定义。
- 编译器检出代码错误后自动尝试根据以往相同错误码所对应的可能解决方案列出修补方案列表
- 根据个人代码命名风格提供更好的自动补全体验

需求评审：

1：不完整 不够明确

修改为：

- 系统通过网络获取时间、日历、天气信息，根据节假日情况更换本地存储的UI壁纸，根据天气阴晴分别将UI的颜色模式改为明亮和暗色。当然，如果用户不喜欢可以自行手动更换壁纸和颜色模式。

2：不完整 

修改为：

- 系统会记录各个用户的代码编译错误以及后续更正的情况，并且上传至云，进行分类存储，后续系统会通过大数据来对当前IDE遇到的编译错误进行匹配，将可能对应的解决方案列出以供用户选择。

3：不完整 不明确

修改为：

- 编译器根据用户当前输入的未完整词，进行联想，列出可能的类名、函数名、变量名等以供用户选择。）
- 编译器会在后台存储一个待匹配的代码风格。此风格有两种来源，一种是用户自己根据已有标准手动定义，一种是根据当前已有输入自动进行风格识别，前者的优先级大于后者。编译器根据待匹配的代码风格，根据当前输入先进行联想得到可能需要的补全代码，再将代码进行风格匹配，将匹配后的补全代码输出，以便用户选择。
